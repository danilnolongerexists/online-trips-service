@include('includes.header')

<div class="container">
    <div class="row">
        <div class="col-md-12">
            <h1 class="my-4">История заказов</h2>
            {{-- @foreach(Auth::user()->receipts as $receipt)

            @endforeach --}}
        </div>
    </div>
</div>
